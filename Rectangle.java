public class Rectangle {

    public static void main(String[] args) {
    	double length, height, area, perimeter;
    	length = 2;
    	height = 5;
    	perimeter = 2 * (length * height);
    	area = length * height;


System.out.println("\nIf the length is equal to " + length + " and the height is equal to " + height + " the perimeter is equal to " + perimeter);
System.out.println("\nIf the length is equal to " + length + " and the height is equal to " + height + " the area is equal to " + area);
    }

}